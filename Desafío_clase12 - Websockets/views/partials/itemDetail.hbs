<article class="d-flex flex-column text-center p-4">
    <h1>{{product.title}}</h1>

    <div>
        <img src={{product.thumbnail}} alt="">
    </div>

    <p>Precio: {{product.price}}</p>

    <a href="/productos" class="btn btn-primary">Ver todos</a>
</article>